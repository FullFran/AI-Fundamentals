---
title: "Introducción a Funciones"
order: 1
description: "Conceptos básicos de funciones matemáticas para IA"
---

# 🧮 Introducción a Funciones

Las matemáticas son el lenguaje de la inteligencia artificial. Antes de entrar en redes neuronales, debemos entender cómo las funciones transforman los datos.

### ¿Qué es una Función?

Una función es una "máquina" que toma una entrada (x) y produce una salida (y). En Deep Learning, casi todo se puede ver como una función gigante.

<InteractiveGraph type="linear" title="Función Lineal: y = kx" />

### Función Sigmoide

Esta función es crucial para la clasificación binaria, ya que comprime cualquier valor al rango (0, 1).

<InteractiveGraph type="sigmoid" title="Sigmoide: Probabilidades" />

```python
import numpy as np

def sigmoid(x):
    return 1 / (1 + np.exp(-x))

print(sigmoid(0)) # 0.5
```

<CollapsibleSection title="Derivación de la Sigmoide">
La derivada de la sigmoide es s(x)(1 - s(x)). Esta propiedad la hizo muy popular en los inicios del Deep Learning.
</CollapsibleSection>
